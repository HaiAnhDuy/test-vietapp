
import { Layout } from 'antd';
const { Header } = Layout;
const HeaderLayout = () => {
    return (
        <>
            <Header style={{ padding: 0, background: '#fff' }} />
        </>
    )
}

export default HeaderLayout